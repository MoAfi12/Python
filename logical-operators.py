"""
temp = int(input("what is the temperature today"))

if temp >= 0 and temp <= 30:
    print("the temprature today is good today")
    print("go to outside")
else:
    print("stay in home")

    """
##when a use the not 
temp = int(input("what is the temprature today"))

if not temp < 0 and temp > 35:
    print("the temprature today is not good stay in hoom")
else:
    print("is good go to out side")